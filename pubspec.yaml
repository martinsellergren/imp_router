name: imp_router
description: "Imperative navigation in flutter. For those who prefer imperative navigation but still want to benefit from Navigation 2.0 features."
version: 0.3.2
repository: https://github.com/martinsellergren/imp_router
issue_tracker: https://github.com/martinsellergren/imp_router/issues
topics:
  - imp-router
  - router
  - navigator

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  animations: ^2.0.11
  collection: ^1.18.0
  provider: ^6.1.1
  rxdart: ^0.27.7
  flutter_web_plugins:
    sdk: flutter
  meta: ^1.11.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
