name: imp_router
description: "A new Flutter package project."
version: 0.0.1
homepage:

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  animations: ^2.0.11
  collection: ^1.18.0
  provider: ^6.1.1
  rxdart: ^0.27.7
  flutter_web_plugins:
    sdk: flutter
  meta: ^1.11.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
