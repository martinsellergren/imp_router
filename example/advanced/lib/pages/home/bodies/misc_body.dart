import 'package:flutter/material.dart';

class MiscBody extends StatelessWidget {
  const MiscBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('empty'));
  }
}
